[package]
name = "spar-sysml2"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "SysML v2 parser — recursive descent with rowan CST"

[dependencies]
rowan.workspace = true

[dev-dependencies]
expect-test = "1"
