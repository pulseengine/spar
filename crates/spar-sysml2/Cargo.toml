[package]
name = "spar-sysml2"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "SysML v2 parser — requirements, constraints, satisfy/verify"

[dependencies]

[dev-dependencies]
